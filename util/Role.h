// This class contains the different type of roles as an ENUM and is used all
// over the application when refering to roles.

#ifndef ROLE_H
#define ROLE_H

enum ROLE { INVALID, ADMIN, TRADER };

#endif
